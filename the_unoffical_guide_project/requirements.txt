sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Ingestion & chunking (Milestone 3 — pipeline.py)
requests>=2.31
beautifulsoup4>=4.12
langchain-text-splitters>=0.3
pdfplumber>=0.11  # extract real text from the BisonHub PDF source
yt-dlp>=2024.0        # download TikTok/YouTube audio (Source 11)
faster-whisper>=1.0   # transcribe audio locally on CPU
curl_cffi>=0.7        # browser impersonation so TikTok serves audio formats
# Rate My Professor is queried directly via its GraphQL API in pipeline.py
# (the RateMyProfessorAPI pip package no longer authenticates), so no extra dep.

# Query interface (Milestone 5)
gradio>=4.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4

# Networking and Parsing
requests>=2.31.0
pypdf>=4.0.0

# LangChain Ecosystem
langchain>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.0
langchain-huggingface>=0.0.3

